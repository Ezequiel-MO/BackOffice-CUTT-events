describe("form", () => {
  it("v- should render available hotels in the DB in a particular location", () => {});
  it("v-input should capture hotels selected from the DB", () => {});
  it("on submit, it should render a form with hotel rates for each hotel selected", () => {});
});
