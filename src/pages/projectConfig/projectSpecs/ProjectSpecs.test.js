describe("form", () => {
  it("v- should capture required fields", () => {});
  it("v- fields should be validated before rendering", () => {});
  it("date fields must be validated - ie, check that start date is in the future, and departure day is after start date", () => {});
});

describe("submit", () => {
  it("v- should make sure Code is not duped in the DB", () => {});
  it("v-if the code is Duped, it should send a message", () => {});
  it("v-should inject captured fields in the DB", () => {});
  it("v-should dispatch Project Status to the Active Code", () => {});
});
