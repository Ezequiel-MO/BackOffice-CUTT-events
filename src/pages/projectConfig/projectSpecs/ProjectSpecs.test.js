describe("form", () => {
  it("should capture required fields", () => {});
  it("fields should be validated before rendering", () => {});
});

describe("submit", () => {
  it("should make sure Code is not duped in the DB", () => {});
  it("if the code is Duped, it should send a message", () => {});
  it("should inject captured fields in the DB", () => {});
  it("should dispatch Project Status to the Active Code", () => {});
});
