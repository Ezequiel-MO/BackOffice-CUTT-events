describe("dashboard", () => {
  it("v-Should render 2 buttons - each button is a module", () => {});
});

describe("Create/Maintain Vendor Module", () => {
  it("v- if the See List of Projects Module is open, it should close it", () => {});
  it("v- on Click, it should render 4 x buttons, with titles : HotelDB, TransportationDB, EventsDB, RestaurantsDB", () => {});
  it("v- on Second click, it should hide all 4 x buttons", () => {});
});

describe("Hotel DB Module", () => {
  it("v-on Click, it should navigate to the hotel-master-form url", () => {});
});

describe("Transportation DB Module", () => {
  it("v-on Click, it should navigate to the transfers-master-form url", () => {});
});

describe("Events DB Module", () => {
  it("v-on Click, it should navigate to the events-master-form url", () => {});
});

describe("Restaurants DB Module", () => {
  it("v-on Click, it should navigate to the restaurants-master-form url", () => {});
});

describe("See List Of Projects", () => {
  it("v- if the Create/maintain Vendor Module is open, it should close it", () => {});
  it("v-should render List of Projects, as a page list (not modal)", () => {});
  it("v-on Second click, it should hide the list", () => {});
});
