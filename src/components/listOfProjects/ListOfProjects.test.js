describe("modal", () => {
  it("when clicked outside of modal, it should hide", () => {});
});

describe("table", () => {
  it("should show a list of projects from the DB", () => {});
  it("should be sortable by date, ascending and descending", () => {});
  it("should be searchable by Code, User or Client", () => {});
});

describe("table + buttons", () => {
  it("when clicked, it should show a briefing of the selected project", () => {});
});

describe("New Project Button", () => {
  it("v- should direct to the project-specs route", () => {});
  it("v- should close the modal when clicked", () => {});
  it("v- should set the project status to creating-project", () => {});
});

describe("Cancel Button", () => {
  it("v- should close the modal when clicked", () => {});
  it("v- should set the project status to initial", () => {});
});
