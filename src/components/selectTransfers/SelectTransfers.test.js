describe("add Transfers", () => {
  it("v-on submit, it should render the current city of the project", () => {});
  it("v-the first select should filter transfer vendors by city in the project", () => {});
  it("the second select should give options for bus capacity available for the vendor", () => {});
  it("the third select should give options for kind of service available for that vendor", () => {});
  it("should render a transfer to be added to the activity", () => {});

  it("a counter should give options for number of vehicles", () => {});
});
