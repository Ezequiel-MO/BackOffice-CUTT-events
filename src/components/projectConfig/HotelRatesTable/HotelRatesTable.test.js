describe("table", () => {
  it("v-should render tabs with selected Hotels", () => {});
  it("v-should render forms of hotels which are active when the tab is active", () => {});
  it("v- On submit, it should aggregate all hotel values in an array of objects", () => {});
  it("v-when all hotels are saved, it should save the array of objects to the project", () => {});
  it("v-it should navigate to /morning-events", () => {});
});
